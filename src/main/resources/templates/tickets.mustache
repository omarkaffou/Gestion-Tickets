{{>layout/header}}
<!-- Page Heading -->

<h1 class="h3 mb-2 text-gray-800" id="title-tickets">Liste des incidents</h1>

<!-- DataTales Example -->
<div class="card shadow mb-4">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-light table-striped" id="dataTable" width="100%" cellspacing="0">
                <thead>
                    <tr>
                        <th>Urgence</th>
                        <th>Status</th>
                        <th>Systeme d'exploitation</th>
                        <th>Issued from</th>
                        <th>Développeur</th>
                        <th>Description</th>
                    </tr>
                </thead>
                <tbody>
                    {{#tickets}}
                    <tr>
                        <td>{{urgence}}</td>
                        <td>{{status}}</td>
                        <td>{{os}}</td>
                        <td>{{client}}</td>
                        <td>{{dev}}</td>
                        <td>{{description}}</td>
                    </tr>
                    {{/tickets}}
                </tbody>
            </table>
        </div>
    </div>
</div>
{{>layout/footer}}